server:
  ssl:
    key-store: tomcat/keystore/keystore.p12

logging:
  level:
    nl:
      blokjeom: debug
    org.springframework.mail: debug

database:
  url: "jdbc:postgresql://localhost:5432/blokje_om_api"
  username: "postgres"
